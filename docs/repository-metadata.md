# Repository Metadata

When this package is published as an independent GitHub repository, configure
the repository sidebar with:

- Description: `Pure Python generation of ICON-style triangular grids`
- Website: `https://ofuhrer.github.io/icon-grid-generator/`
- Topics: `python`, `icon`, `grid`, `geodesic`, `triangular-grid`, `netcdf`,
  `atmospheric-science`, `scientific-computing`

Recommended GitHub features:

- Enable Issues and Discussions if external users are expected.
- Enable branch protection for `main` once CI is active.
- Publish GitHub Releases using the entries from `CHANGELOG.md`.
- Connect the repository to Zenodo before the first public release if DOI-based
  citation is required.
